# 04 仕様書(技術者向け)

将来このシステムを修正する人(またはAIアシスタント)向けの設計資料です。

## 1. アーキテクチャ

- Google Apps Script(V8ランタイム)のウェブアプリ。スプレッドシートにバインドされた
  コンテナバインドスクリプト
- フロントエンドは `index.html` + `style.html` + `script.html` の単一ページ構成。
  `google.script.run` でサーバー関数(`api〜`)を呼ぶ
- データストアはスプレッドシートの6シート。ORMはなく、列位置の定数
  (`Code.gs` の `ROSTER_COL` / `GRANT_COL` / `USAGE_COL`)で読み書きする
- 書き込みを伴う処理はすべて `withLock_()`(ScriptLock)内で行い、同時操作による
  不整合を防ぐ
- **メール通知は現在停止中**(`Notify.gs` の `MAIL_ENABLED = false`)。
  この間は通知キュー・通知ログのシートもトリガーも作られず、`publicSettings_` が
  返す `mailFeature: false` を見て設定画面のメール項目も隠れる。
  再開する場合は `MAIL_ENABLED` を `true` にして初期セットアップを実行する。
  以下は再開したときの仕様:
- **メールは同期送信しない**。`enqueueMails_` で「通知キュー」シートに積み、
  1分ごとの時間主導トリガー `processMailQueue` が最大20通ずつ送る。
  `MailApp.sendEmail` は1通あたり数秒かかり、リクエスト内で送るとタイムアウトの
  原因になるため。キュー処理は ScriptLock ではなく DocumentLock を使い、
  申請・承認の処理を待たせない。トリガーが無い環境では `enqueueMails_` が
  6時間に1回だけ自動設置を試み、失敗時はその場で同期送信にフォールバックする
- 応答速度のため、GASのサービス呼び出しを1実行あたり1回に抑えている:
  `tz_()`/`ss_()`/`appUrl_()` はメモ化、`normDateStr_` は文字列入力なら
  `Utilities.formatDate` を経由しない、`updateCells_` は1行を1回の読み書きで更新、
  ID採番は読み込み済みの行を受け取れる(`nextIds_(..., rows)`)
- 表示用データは `apiGetAllData` が役割に応じて**全タブ分を1回で**返し、
  ログイン応答(`apiLogin`)にも同梱される。フロントエンドはこれを**3分間キャッシュ**
  する(`script.html` の `cachedApi`)ため、タブ切替で通信は発生しない。
  申請・承認・名簿変更などの更新系操作の成功時とヘッダーの「更新」ボタンで
  キャッシュを破棄して取り直す。残高チェックや承認の確定は常にサーバー側で
  最新のシートに対して行われるため、キャッシュが影響するのは表示の鮮度のみ
  (他人の操作が最大3分遅れて見える)
- 日付・時刻はシート上では文字列(`yyyy-MM-dd` / `H:mm`)。セットアップ時に該当列へ
  テキスト書式(`@`)を設定し、読み出し時にも `normDateStr_` / `normTimeStr_` で正規化する
  (Sheetsの自動型変換への二重の防御)。`google.script.run` は Date を返せないため、
  APIの戻り値はプリミティブのみで構成する

## 2. シート構造

### 設定(キー・バリュー)
| 項目 | 値の例 | 説明 |
|---|---|---|
| 学校名 | ○○小学校 | 表示用 |
| 年度 | 2026 | 年度切替で+1 |
| 勤務開始時刻 / 勤務終了時刻 | 8:30 / 17:00 | 算定の基準 |
| 繰越失効日 | (空) / 2026-06-30 | **空=繰越有効**。値が入ると繰越残は常に0として計算 |
| 管理職への依頼メール / 本人への結果メール | ON / OFF | 通知設定 |

### 名簿
`ID(T001…) / 氏名 / 役職(管理職・主任・教員) / PIN / メールアドレス / 状態(在籍・停止) / 繰越時間(分) / 備考`

- PINは平文では保存しない。`#`+64桁16進のソルト付きSHA-256ハッシュ
  (`hashPin_`: `memberId + ':' + pin + ':' + PIN_SECRET` のダイジェスト)で保存する。
  `PIN_SECRET` はスクリプトプロパティに初回アクセス時自動生成される
- PIN欄に**平文を直接書いた場合も動作**し、次回ログイン成功時に自動でハッシュへ
  置き換えられる(`verifyPin_` の遅延移行)。これはバックアップ復元などで
  `PIN_SECRET` が失われた際の復旧経路を兼ねる(シートのコピーにスクリプト
  プロパティは引き継がれないため、復元後は全ハッシュが照合不能になる。
  その場合は名簿に平文PINを書いてログインすれば新しい鍵で再ハッシュされる)
- 「繰越時間(分)」は年度切替時に自動計算で書き込まれる(年度中の手修正も可)
- 残時間・履歴はすべて**ID**に紐づく(氏名は表示用)。名簿は年度切替でも削除されない。
  新規メンバーのIDは、名簿だけでなく付与・利用記録に残っているIDとも重複しないよう
  発行する(`nextMemberIds_`。行削除済みメンバーの記録を別人が引き継ぐ事故の防止)
- 名簿画面の操作は目的別API(追加/役職・連絡先/氏名修正/状態切替/PIN初期化)に
  分かれており、どの操作でも**IDと繰越時間は変更できない**。年度初めの人事異動は
  年次更新ウィザード側で行う想定(3〜4月は名簿画面に警告を表示)

### 付与記録(1行 = 1教職員への1件の付与)
`付与ID(G00001…) / グループID(GG0001…) / 状態 / 発生日 / 開始時刻 / 終了時刻 / 付与分数 / 事由 / 対象者ID / 対象者氏名 / 起案者ID / 起案者氏名 / 申請日時 / 処理者氏名 / 処理日時 / 処理メモ / 取消者氏名 / 取消日時`

- 1回の起案(会議1件)で対象者の人数分の行が作られ、同じグループIDを持つ

### 利用記録
`利用ID(U00001…) / 状態 / 取得日 / 開始時刻 / 終了時刻 / 利用分数 / 繰越充当分 / 今年度充当分 / 教員ID / 教員氏名 / 備考 / 申請日時 / 処理者氏名 / 処理日時 / 処理メモ / 取消者氏名 / 取消日時`

- 繰越充当分・今年度充当分は**承認時**に確定して書き込まれる(申請時は空)

### 通知キュー
`登録日時 / 種別 / 宛先 / 件名 / 本文 / 状態(未送信・送信済み・失敗) / 処理日時`
送信待ちのメール置き場。1分ごとの自動処理が送信し、処理済みの古い行は自動で間引かれる。

### 通知ログ
`日時 / 種別 / 宛先 / 件名 / 結果` — メール送信の成否記録(デバッグ用)

## 3. 状態遷移

```
承認待ち ──(管理職)──> 承認済み ──(管理職)──> 取消
   │
   ├─(管理職)──> 却下
   └─(本人/起案者)──> 取下げ
```

- 行は削除しない。すべて状態変更で表現する(監査可能性のため)
- 残時間に影響するのは「承認済み」のみ(取消は除外される)

## 4. 時間の算定ルール(Logic.gs)

- 入力は5分単位・開始<終了を `validateTimeRange` で検証
- 付与分数 = 入力時間帯 −(入力時間帯 ∩ 勤務時間帯) … 勤務時間**外**のみ
- 利用分数 = 入力時間帯 ∩ 勤務時間帯 … 勤務時間**内**のみ
- 0分になる入力は申請時にエラー
- 算定分数は申請時に行へ記録し、以後は再計算しない
  (後から勤務時間設定を変えても過去の記録は変わらない)

### 二重登録の防止

通信エラーで申請ボタンを押し直しても同じ記録が増えないよう、登録前に同一内容を検査する
(`isDuplicateGrant` / `isDuplicateUsage`。ロックの内側で実行するので競合しない)。

- 付与: 対象者ID・発生日・開始・終了・事由がすべて一致
- 利用: 教員ID・取得日・開始・終了が一致
- 状態が「承認待ち」「承認済み」のものだけが対象(`isLiveStatus`)。
  却下・取下げ・取消になった申請は同じ内容で出し直せる
- 付与で一部の対象者だけが重複した場合は、新しい対象者のみ登録し `skipped` で返す。
  全員が重複した場合はエラーとして「すでに登録済み」を伝える(登録は行わない)
- 画面側も送信中はボタンを無効化し、通信エラー時は「押し直さずに更新して確認」を促す

## 5. 残時間の計算(Code.gs: computeBalanceMap_)

教職員ごとに:

```
繰越残   = 失効済み ? 0 : max(名簿.繰越時間 − Σ承認済み利用.繰越充当分, 0)
今年度残 = Σ承認済み付与.付与分数 − Σ承認済み利用.今年度充当分
残り合計 = 繰越残 + 今年度残
利用可能 = 残り合計 − Σ承認待ち利用.利用分数
```

- 申請時のチェックは「利用可能」で行う(承認待ちの利用を二重に申請できない)
- 承認時に `allocateUsage`(Logic.gs)で **繰越優先** に充当し、内訳を行に書き込む。
  不足していれば承認はエラーになる(同一バッチ内の複数承認も逐次減算で整合)
- 前年度分と今年度分を区別することで、繰越失効後に今年度残がマイナスになる事故を
  構造的に防いでいる(tests/balance.test.js に要件由来のシナリオテストあり)
- 「今年度残」は付与の強制取消によって負になり得る(管理画面で赤字表示)

## 6. 年次更新

### 年度切替(apiAdminYearSwitch)
1. 人事異動の入力内容と承認待ちを検証(問題があれば何も変更せず中止)
2. `Spreadsheet.copy()` で全体バックアップ(ドライブに保存)
3. 各自の `max(残り合計, 0)` を名簿の「繰越時間(分)」へ書き込み
4. 付与記録・利用記録・通知ログのデータ行を全消去、年度+1、繰越失効日をクリア
5. 人事異動を反映: 転出者(deactivateIds)を「停止」に、転入者(newMembers)を追加し
   初期PINを発行して応答で一度だけ返す

UIは「年次更新」タブのウィザード(`script.html` の `state.wizard`)が
作業選択 → 事前チェック → 人事異動入力 → 最終確認 → 完了 の一本道で案内する。
失効も同じウィザードから実行する。

### 繰越失効(apiAdminExpireCarryover)
- 設定の「繰越失効日」に当日を書くだけ。以後の計算で繰越残が常に0になる
- 取り消したい場合はセルを空にすれば元に戻る(データ自体は変更していないため)
- 失効後に繰越充当を含む利用を取り消した場合、繰越分の充当は復元されない
  (失効済みのため。今年度充当分のみ戻る)

## 7. 認証・セッション

- ログイン: 名簿のIDとPINの照合(在籍のみ・ハッシュ比較、旧平文は成功時に自動移行)。
  5回失敗で5分ロック(CacheService)。PINの初期化(`apiAdminResetPin`)でロックも解除
- PINそのもの・ハッシュ値はクライアントへ一切送らない(管理画面には設定済み/未設定のみ)
- セッション: **署名付きの自己完結トークン**(サーバー側に保存しない)。
  形式は `memberId.発行時刻(ms).SHA-256(memberId.発行時刻.SESSION_SECRET)`。
  検証は署名の再計算と経過時間チェックのみ(`parseToken_`)。有効期間は12時間。
  クライアントは localStorage に保持し、全APIの第1引数で送る
  - 以前は CacheService にトークンを保存していたが、CacheService は
    いつ破棄されてもよい一時領域(かつ有効期限の上限が6時間)のため、
    ログイン直後でも「有効期限が切れました」になる不具合が起きた。
    保存をやめたことでこの failure mode は構造的に消えている
  - 保存しない代わりにサーバー側から個別トークンを失効させることはできない。
    ログアウトはクライアントの localStorage を消す動作(有効期限が来れば失効)
  - `SESSION_SECRET` は `PIN_SECRET` とは別に自動生成される(`scriptSecret_`)
- セッション切れはサーバーが `AUTH:` 接頭辞付きエラーを投げ、クライアントが
  ログイン画面へ戻す
- 権限: `requireGranter_`(管理職・主任)、`requireAdmin_`(管理職)。
  役職は毎リクエスト名簿から再取得する(役職変更が即時反映される)
- ウェブアプリは「全員(匿名)」公開・「自分として実行」。スプレッドシートへの
  直接アクセス権はユーザーには無く、APIが返すものだけが見える

## 8. API一覧(google.script.run)

| 関数 | 権限 | 内容 |
|---|---|---|
| apiGetLoginList() | なし | 在籍者の名前一覧(ログイン画面用) |
| apiLogin(id, pin) / apiLogout(token) | なし/本人 | 認証(成功時は全タブ分のデータを同梱して返す) |
| apiChangePin(token, old, new) | 本人 | PIN変更 |
| apiGetAllData(token) | 本人 | 全タブ分の表示データ一式(役割に応じて付与対象・管理データを含む) |
| apiGetMyPage(token) | 本人 | (旧画面との互換用)残時間・承認待ち・最近の申請 |
| apiGetMyRecords(token) | 本人 | (旧画面との互換用)自分の全履歴 |
| apiGetGrantTargets(token) | 主任以上 | (旧画面との互換用)付与対象の名簿 |
| apiSubmitGrant(token, payload) | 主任以上 | 付与の起案(複数人へ一括) |
| apiSubmitUsage(token, payload) | 本人 | 利用の申請(残高チェックあり) |
| apiWithdraw(token, kind, id) | 本人/起案者 | 承認待ちの取下げ |
| apiAdminGetAll(token) | 管理職 | 管理画面用の全データ |
| apiAdminDecideMany(token, {grantIds,usageIds}, action, memo) | 管理職 | 承認/却下。付与・利用を混在させて一括処理でき、付与を先に反映するので「付与→その分の利用」を同時に承認できる。部分失敗は errors で返す |
| apiAdminDecide(token, kind, ids, action, memo) | 管理職 | (旧画面との互換用)片方の種別だけの承認/却下 |
| apiAdminCancel(token, kind, id, force) | 管理職 | 承認済みの取消(マイナス時は needConfirm) |
| apiAdminAddMember(token, member) | 管理職 | 教職員の追加(初期PINを一度だけ返す) |
| apiAdminUpdateContact(token, member) | 管理職 | 役職・メール・備考の変更 |
| apiAdminRenameMember(token, id, name) | 管理職 | 氏名の修正(同一人物のみ。過去記録の表示名は不変) |
| apiAdminSetMemberStatus(token, id, status) | 管理職 | 在籍/停止の切替(休職・復職) |
| apiAdminSaveMember(token, member) | 管理職 | (旧画面との互換用)名簿の追加・更新 |
| apiAdminResetPin(token, memberId) | 管理職 | PIN初期化(新しい6桁PINを生成し一度だけ返す) |
| apiAdminSaveSettings(token, settings) | 管理職 | 設定保存 |
| apiAdminYearSwitch(token, '年度切替', personnel) | 管理職 | 年度切替+人事異動の一括反映(転入者の初期PINを一度だけ返す) |
| apiAdminExpireCarryover(token, '失効') | 管理職 | 繰越失効 |

## 9. 制約・前提

- GASクォータ: メール送信 無料100通/日・Workspace 1,500通/日。実行時間 6分/回。
  数十人規模・年数千行程度なら全く問題ない
- 同時アクセスはScriptLock(最大20秒待ち)で直列化される
- 名前一覧はログイン画面(URLを知っていれば誰でも)に表示される。
  URLの管理が前提のセキュリティモデル
- 過年度データは年度切替時のバックアップコピーが正本

## 10. テスト

```
node tests/logic.test.js    # 時間算定・充当・表記(Logic.gs)
node tests/balance.test.js  # 残時間計算・失効シナリオ(Code.gs)
```

GASサービスに依存する部分(シートI/O・承認フロー)は、
[01 セットアップ手順](01_セットアップ手順.md) の動作確認チェックリストで手動確認する。
